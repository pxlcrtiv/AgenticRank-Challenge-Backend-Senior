{
  "name": "quickbite-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migrate": "npx knex migrate:latest",
    "migrate:test": "npx knex migrate:latest --env test",
    "seed": "npx knex seed:run",
    "seed:test": "npx knex seed:run --env test",
    "setup": "npx knex migrate:latest && npx knex seed:run",
    "test": "jest --runInBand --forceExit",
    "test:watch": "jest --runInBand --forceExit --watch"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFilesAfterSetup": [],
    "testMatch": ["**/*.test.js"]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "knex": "^3.1.0",
    "morgan": "^1.10.1",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
